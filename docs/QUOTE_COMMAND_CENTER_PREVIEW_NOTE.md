# Quote Command Center design preview note

This branch adds a presentation-only design route at `/design/quote-command-center` plus the UX implementation spec.

It does **not** change quote calculations, Stripe/payment behavior, tenant authorization, database schema, Merchant Console production screens, or customer widget logic.

The preview exists so Emmanuel and ChatGPT can visually approve the command-center and PDF direction before Claude Code implements the tenant audit and document backend.
